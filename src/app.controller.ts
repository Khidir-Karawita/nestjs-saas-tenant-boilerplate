import { Controller, Get } from '@nestjs/common';
import { AppService } from './app.service';

/**
 * Main application controller that handles root-level endpoints.
 * This controller provides basic application health and status endpoints.
 */
@Controller()
export class AppController {}
